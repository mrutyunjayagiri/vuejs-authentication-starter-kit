export const getters = {
    authenticated(state) {
        return state.authenticated
    },
    authenticatedToken(state) {
        return state.authenticatedToken
    },
    authenticatedData(state) {
        return state.authenticatedData
    }
}